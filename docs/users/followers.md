# Followers Count

Request Method: GET

* */users/`userID`/followers/count* = Get the follower count of channelID
  * Response Example:

     ```js
     {"count":1337} // Follower Count
     ```
  